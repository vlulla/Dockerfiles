name: build-geospatial-python-docker-image
on: 
  push:
    branhces:
      - main
    paths:
      - python_geospatial/**
      - scipy_python/**
jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-repo
        uses: actions/checkout@v2
      - name: default-make-rule
        run: cd python_geospatial && make


